NODE mv
DESCRIPTION >
    grouping the sales per product

SQL >

    SELECT
        product,
        sumSimpleState(-1) AS available_stock
    FROM confluent_events
    WHERE event = 'sale'
    GROUP BY product

TYPE materialized
DATASOURCE mv_latest_availability


